packages:
  - apps/meeting-bot-page
  - packages/*

allowBuilds:
  '@railway/cli': true
  '@twilio/cli-core': true
  twilio-cli: true
  yarn: false
  esbuild: true
  supabase: true

# The Lovable frontend remains at the workspace root (ADR-0002).

overrides:
  rolldown: 1.2.1
